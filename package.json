{
  "name": "shadow-depths",
  "version": "0.1.0",
  "description": "Shadow Depths — a turn-based roguelike dark fantasy. Foundation build (v0.1).",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "lint": "eslint src --ext .js",
    "deploy": "vercel --prod",
    "deploy:preview": "vercel",
    "sim": "node scripts/sim.js"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "vite": "^5.4.0"
  }
}
